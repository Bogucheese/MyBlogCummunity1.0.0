# ----------------------------------------
# 1. 数据库连接配置 (MySQL)
# ----------------------------------------

# 数据库连接URL：
# 已使用您提供的IP、端口和用户（将数据库名设置为WEBSITE）
spring.datasource.url=jdbc:mysql://129.28.35.152:3306/WEBSITE?useSSL=false&serverTimezone=Asia/Shanghai
# 数据库用户名
spring.datasource.username=WEBSITE
# 数据库密码
spring.datasource.password=Z9EMNn6hO
# 连接池驱动类名
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
server.port=1234
# ----------------------------------------
# 2. JPA/Hibernate 配置
# ----------------------------------------

# 已移除 spring.jpa.database-platform，让 Spring Boot 自动推断方言。
# 这解决了您之前遇到的 [org.hibernate.dialect.MySQLDialect ] 找不到的错误。
# 如果仍然报错，请检查您的 pom.xml 中是否有 spring-boot-starter-data-jpa 依赖。

# Hibernate DDL 模式
spring.jpa.hibernate.ddl-auto=update

# 打印 SQL 语句，方便调试 (开发时常用)
spring.jpa.show-sql=true